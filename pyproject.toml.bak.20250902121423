[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
# E=pycodestyle, F=pyflakes, I=isort, UP=pyupgrade, B=bugbear
select = ["E", "F", "I", "UP", "B"]
ignore = ["E203"]  # Black互換が必要なら維持、不要なら消してOK

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["wanstage"]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
line-ending = "lf"
