{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Shorten (last_post.json)",
      "type": "shell",
      "command": "jq -r '.short_code' $HOME/WANSTAGE/logs/last_post.json | xargs -I{} echo http://127.0.0.1:3000/{}"
    }
  ]
}
