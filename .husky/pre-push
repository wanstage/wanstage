#!/usr/bin/env sh
set -euo pipefail

echo "[pre-push] TypeScript typecheck"
npm run -s check:ts

echo "[pre-push] ESLint"
npm run -s check:js

echo "[pre-push] HTML validate"
npm run -s check:html

echo "[pre-push] Python (ruff/black)"
npm run -s check:py

echo "[pre-push] OK"
